import React from "react";

const HeaderMenus: React.FC = (): JSX.Element => {
  const setShowStudentHelpDoc = (status: boolean) => {};

  const handleShowHelpModal = () => {};

  return (
    <></>
  );
};

export default HeaderMenus;
